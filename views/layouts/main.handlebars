<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">

    {{!-- <link rel="stylesheet" href="/mvc-tech-blog/public/css/jass.css"> --}}
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="flex-column justify-space-around"></div>
    <header class="display-flex text-center justify-space-between align-center p-2">
        <h1>
            <span role="img" aria-label="computer">🖥️</span>
            <a href="/">My Tech Blog!</a>
            <span role="img" aria-label="computer">🖥️</span>
        </h1>
        <nav>
            <!-- Conditionally render login or logout links -->
            {{#if logged_in}}
            <a href="/">Home</a>
            <a href="/dashboard">Dashboard</a>
            <button class="no-button" id="logout">Logout</button>
            {{else}}
            <a href="/">Home</a>
            <a href="/dashboard">Dashboard</a>
            <a href="/login">Login</a>
            {{/if}}
        </nav>
    </header>
    <main {{!-- class="container container-fluid mt-5" --}}>
        <!-- Render the sub layout -->
        {{{ body }}}
    </main>
    <footer>

    </footer>
    </div>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>

</html>